global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "taskly-api"
    metrics_path: /metrics/
    static_configs:
      - targets: ["api:8000"]
